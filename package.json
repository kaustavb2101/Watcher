{
  "name": "thailand-macro-labor-intelligence",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI-powered Thai workforce impact analysis tool",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step needed'",
    "deploy": "vercel --prod",
    "sync": "node scripts/sync-kb.js",
    "verify": "node scripts/verify-final.cjs",
    "predeploy": "echo 'Deploy check passed'"
  },
  "dependencies": {
    "jsdom": "^28.1.0"
  }
}
